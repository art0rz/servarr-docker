# Basics
TZ=Europe/Stockholm
PUID=1000
PGID=1001
LAN_SUBNET=192.168.86.0/24
MEDIA_DIR=/mnt/media

# Docker Configuration (Optional - auto-detected by bootstrap.sh)
# Uncomment to override auto-detection:
# DOCKER_GID=984

# Project name (defaults to directory name if not set)
# COMPOSE_PROJECT_NAME=servarr

# qBittorrent Configuration
QBIT_WEBUI=8080

# VPN Configuration (set to false to disable VPN)
USE_VPN=true

# VPN Provider Configuration (only used if USE_VPN=true)
# See https://github.com/qdm12/gluetun-wiki for all supported providers
VPN_SERVICE_PROVIDER=protonvpn
VPN_TYPE=wireguard

# Server selection (provider-specific)
# Examples: SERVER_COUNTRIES, SERVER_CITIES, SERVER_HOSTNAMES
SERVER_COUNTRIES=Sweden
SERVER_CITIES=Stockholm
# To pin a specific server hostname:
# SERVER_HOSTNAMES=se-41.protonvpn.net

# VPN Credentials (provider-specific - examples below)
# ProtonVPN WireGuard:
WIREGUARD_PRIVATE_KEY=YOUR_PRIVATE_KEY
WIREGUARD_ADDRESSES=YOUR_WIREGUARD_ADDRESS
# OpenVPN providers (NordVPN, ExpressVPN, etc.):
OPENVPN_USER=
OPENVPN_PASSWORD=
# See Gluetun docs for other providers

# Service Ports
PROWLARR_PORT=9696
SONARR_PORT=8989
RADARR_PORT=7878
BAZARR_PORT=6767
FLARESOLVERR_PORT=8191
CROSS_SEED_PORT=2468
HEALTH_PORT=3000

# Health Server Configuration
# Service IPs and ports are auto-discovered from Docker containers
# Whitelist these Docker network subnets in *arr apps and qBittorrent WebUI settings:
#   172.18.0.0/16 (default network)
#   172.19.0.0/16 (media network)
